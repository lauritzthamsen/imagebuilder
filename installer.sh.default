#!/bin/bash

# Put your own values here
USER="aUser"
PW="aPw"
VM="/aPath/to/vm"
CONFIG="ConfigurationOf???" 
REPOSITORY="http://www.address.com/Repository"

./installer.sh -u "$USER" -p "$PW" -v "$VM" -c "$CONFIG" -r "$REPOSITORY"