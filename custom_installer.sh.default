#!/bin/bash

### Configuration

# Path to your Squeak VM 
VM="/path/to/vm"

# Monticello repository and Credentials
REPOSITORY="http://www.squeaksource.com/SQUEAKSOURCE_REPOSITORY"
USER=""
PW=""

# Metacello configuration that configures the image to personal preferences
CONFIG="ConfigurationOfLauritz" 

### Installer execution

# Add the parameterlesss '-a' switch to create an archive of the installed files
`dirname $0`/installer.sh -u "$USER" -p "$PW" -v "$VM" -c "$CONFIG" -r "$REPOSITORY"